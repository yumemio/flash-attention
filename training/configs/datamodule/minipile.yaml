_target_: src.datamodules.language_modeling_hf.LMDataModule
dataset_name: minipile
dataset_config_name: null
tokenizer_name: gpt2
cache_dir: ${oc.env:DATA_DIR,${data_dir}}/minipile/cache
max_length: 2048
add_eos: True
batch_size: 4  # per GPU
batch_size_eval: ${eval:${.batch_size} * 2}
num_workers: 32  # For preprocessing only
shuffle: True
pin_memory: True
__train_len: ${div_up:1696757911, ${.max_length}}
